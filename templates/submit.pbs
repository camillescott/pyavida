#!/bin/sh -login

#PBS -l {{ resources }}
#PBS -e {{ folder }}/{{ jobname }}.e
#PBS -o {{ folder }}/{{ jobname }}.o
#PBS -N {{ jobname }}
{% if buyin %}
#PBS -A {{ buyin }}
{% endif %}

cd {{ folder }}

./avida -s {{ seed }}
{% if analyze %}
./avida -a
{% endif %}
